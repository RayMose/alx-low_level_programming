
#include <stdio.h>

/*
 *  * main -Prints a hexadecimal number
 *   *
 *    *Return: Always (Success).
 *
 *     */
int main(void)
{

	    char c;

	        for (n = '0'; c <= '9'; c++)
			        putchar(c);
		    for (n = 'a'; c <= 'f'; c++)
			            putchar(c);

		        putchar('\n');

			    return (0);
}
